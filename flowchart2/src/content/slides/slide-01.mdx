import { LogoImage } from "@/components/media/logo-image";

<div className="flex h-full w-full flex-col">
  <div className="flex flex-1 flex-col items-center justify-center gap-6">
    <div className="flex flex-wrap items-center justify-center gap-6">
      <LogoImage brand="chatobserver" className="h-16 w-auto" priority />
      <span className="h-10 w-px bg-[color-mix(in srgb, var(--foreground) 40%, transparent)]" aria-hidden />
      <LogoImage brand="clean-digital" className="h-10 w-auto" priority />
    </div>

    <SlideHeader align="center" kicker="">
      ChatObserver — Spin‑Out Proposal
    </SlideHeader>

    <SlideSubhead className="text-center text-[color-mix(in srgb, var(--foreground) 80%, transparent)] max-w-2xl">
      A safer, faster way for Clean Digital to capture upside without carrying product risk
    </SlideSubhead>
  </div>
</div>
