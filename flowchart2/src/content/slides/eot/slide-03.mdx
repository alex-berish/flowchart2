<div className="flex h-full w-full flex-col">
  <div className="flex flex-1 flex-col gap-10 px-8 py-6 w-full max-w-4xl mx-auto">
    <SlideHeader eyebrow="Operating Model" kicker="Structure that passes the EOT test">
      Operating Model That Works for the Trust
    </SlideHeader>

    <SlideBody className="grid gap-8 text-left">
      <div className="flex flex-col gap-3">
        <p className="text-sm uppercase tracking-[0.2em] text-[color-mix(in srgb, var(--foreground) 65%, transparent)]">
          Structure
        </p>
        <BulletList className="space-y-3">
          <li>
            <span className="font-semibold text-[var(--foreground)]">Alex runs ChatObserver outside CD</span> (founder-led cadence).
          </li>
          <li>
            <span className="font-semibold text-[var(--foreground)]">CD participates as the channel</span> for CD-sourced clients.
          </li>
        </BulletList>
      </div>

      <div className="flex flex-col gap-3">
        <p className="text-sm uppercase tracking-[0.2em] text-[color-mix(in srgb, var(--foreground) 65%, transparent)]">
          Economics for beneficiaries
        </p>
        <BulletList className="space-y-3">
          <li>
            <span className="font-semibold text-[var(--foreground)]">Early participation:</span> 100% of early net cash to CD up to a modest cap or trigger.
          </li>
          <li>
            <span className="font-semibold text-[var(--foreground)]">Ongoing participation:</span> 30% in Year-1 / 15% thereafter on CD-sourced accounts.
          </li>
          <li>
            Optional <span className="font-semibold text-[var(--foreground)]">small success kicker</span> (e.g., 2%) only on a bona fide funding event.
          </li>
        </BulletList>
      </div>

      <div className="flex flex-col gap-3">
        <p className="text-sm uppercase tracking-[0.2em] text-[color-mix(in srgb, var(--foreground) 65%, transparent)]">
          Why this serves the trust
        </p>
        <BulletList className="space-y-3">
          <li>
            <span className="font-semibold text-[var(--foreground)]">Probability ↑</span> (speed, focus, accountability).
          </li>
          <li>
            <span className="font-semibold text-[var(--foreground)]">Exposure ↓</span> (product liability and data responsibilities sit outside CD).
          </li>
          <li>
            <span className="font-semibold text-[var(--foreground)]">PPC protected</span> (no headcount pull; no governance drag).
          </li>
        </BulletList>
      </div>
    </SlideBody>
  </div>
</div>
