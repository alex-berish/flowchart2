<div className="flex h-full w-full flex-col">
  <div className="flex flex-1 flex-col gap-10 px-8 py-6 w-full max-w-4xl mx-auto">
    <SlideHeader eyebrow="Operating Model">
      Operating Model
    </SlideHeader>

    <SlideBody className="grid gap-8 text-left">
      <div className="flex flex-col gap-3">
        <p className="text-sm uppercase tracking-[0.2em] text-[color-mix(in srgb, var(--foreground) 65%, transparent)]">
          Structure
        </p>
        <BulletList className="space-y-3">
          <li>
            <span className="font-semibold text-[var(--foreground)]">Alex runs ChatObserver outside CD</span> (dev-led cadence).
          </li>
          <li>
            <span className="font-semibold text-[var(--foreground)]">CD participates as the channel</span> for CD-sourced clients.
          </li>
        </BulletList>
      </div>

      <div className="flex flex-col gap-3">
        <p className="text-sm uppercase tracking-[0.2em] text-[color-mix(in srgb, var(--foreground) 65%, transparent)]">
          Economics for beneficiaries
        </p>
        <BulletList className="space-y-3">
          <li>
            <span className="font-semibold text-[var(--foreground)]">Outcome:</span> 100% of profit on CD-sourced accounts.
          </li>
          <li>
            Optional <span className="font-semibold text-[var(--foreground)]">success kicker</span> on a funding round.
          </li>
          <li>
            Equity option in the event of a VC-led funding round
          </li>
        </BulletList>
      </div>

      <div className="flex flex-col gap-3">
        <p className="text-sm uppercase tracking-[0.2em] text-[color-mix(in srgb, var(--foreground) 65%, transparent)]">
          Why this serves the trust
        </p>
        <BulletList className="space-y-3">
          <li>
            <span className="font-semibold text-[var(--foreground)]">Success Probability ↑</span> (Alex will build and ship 20+ hours a day in NewCo scenario).
          </li>
          <li>
            <span className="font-semibold text-[var(--foreground)]">Exposure ↓</span> (product liability and risk sit outside CD).
          </li>
          <li>
            <span className="font-semibold text-[var(--foreground)]">PPC protected</span> (no headcount pull; no governance drag).
          </li>
        </BulletList>
      </div>
    </SlideBody>
  </div>
</div>
